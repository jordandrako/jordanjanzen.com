import reducer from './reducers';

export { default as skillSelectors } from './selectors';
export { default as skillOperations } from './operations';
export { default as skillTypes } from './types';

export default reducer;
